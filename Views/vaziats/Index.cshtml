@model IEnumerable<komite.Models.vaziat>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.onvan_vaziat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.onvan_vaziat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_vaziat">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_vaziat">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_vaziat">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
