@model komite.Models.onvan_komite

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}

<div id="content" style="position:relative;right:-3000px">
<h4>آیا مایل به حذف اطلاعات می باشید؟</h4>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.name)
        </dt>
        <dd class = "col-sm-10">
            <div style="color:white">
                @Html.DisplayFor(model => model.name)
            </div>
            
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="id_onvan_komite" />
        <input type="submit" value="حذف اطلاعات" class="btn btn-outline-danger btn-rounded" /> |
        <a asp-action="Index">بازگشت به لیست</a>
    </form>
</div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        $(document).ready(() => {
            $("#content").animate({ right: "0" });
            $(window).scrollTop($("#back").offset().top);
        });
    </script>
    <script>
        function move_back() {
            $("#form").submit(function (e) {
                e.preventDefault();
            });
            if (span.show() != true) {
                $("#content").animate({ right: "-3000" });
                $("#form").submit();
            }
        }
    </script>
}
